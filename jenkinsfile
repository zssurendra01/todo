pipeline {
    agent any
    stages {
        stage("Installing npm") {
            steps {
                sh '''
                npm install
                '''
            }
        }
    }
    
        stage("seeing artifacts") {
            steps {
                sh '''
                zip -r todo.zip node_modules server.js *
                '''
            }
        }
    
}